{
  "name": "aws-cert-practice",
  "private": true,
  "version": "0.2.0",
  "description": "Practice app for AWS certification exams with Claude-generated question pools",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "node backend/dist/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
